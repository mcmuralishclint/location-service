google:
  maps_api_key: "${GOOGLE_MAPS_API_KEY}"
test:
  maps_api_key: ''
geoscape:
  maps_api_key: "${GEOSCAPE_MAPS_API_KEY}"
baidu:
  maps_api_key: "${BITBUCKET_PR_DESTINATION_BRANCH}"
address_provider: "geoscape"

cache:
  redis:
    host: "localhost:6379"
    password: ""
    db: 0
  cache_db: "redis"

country: "AU"
port: 3000